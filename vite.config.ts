import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  // Fix the base path for GitHub Pages
  base: '/controller-visualizer/',
})
